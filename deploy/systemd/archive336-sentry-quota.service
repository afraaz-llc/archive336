[Unit]
Description=ARCHIVE336 — Sentry quota tripwire
After=network.target

[Service]
Type=oneshot
User=root
WorkingDirectory=/opt/aether/app/backend
EnvironmentFile=/opt/aether/.env
ExecStart=/opt/aether/venv/bin/python -m scripts.check_sentry_quota
# Hardening to match archive336-api.service
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/opt/aether
